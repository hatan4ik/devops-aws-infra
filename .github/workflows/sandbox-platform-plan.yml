name: Sandbox platform plan

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/sandbox-platform-plan.yml
      - .github/workflows/_terraform-root-plan.yml
      - infra/active/roots/sandbox-platform/us-east-2/dev/**
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: sandbox-platform-plan-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # Delegates fork rejection (`github.event.pull_request.head.repo.full_name == github.repository`) and terraform plan execution to the reusable workflow.
  plan:
    name: sandbox_platform_plan
    uses: ./.github/workflows/_terraform-root-plan.yml
    with:
      root_directory: infra/active/roots/sandbox-platform/us-east-2/dev
      aws_region: us-east-2
      role_variable: AWS_SANDBOX_PLATFORM_PLAN_ROLE_ARN
      session_prefix: sandbox-platform
      job_name: sandbox_platform_plan
