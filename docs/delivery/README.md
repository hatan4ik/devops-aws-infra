# Delivery lane

This directory defines the order in which the candidate design becomes an
operable platform. It does not grant AWS permissions or replace a reviewed
change record.

The current entry point is the [first delivery slice](first-delivery-slice.md).
